{
  "name": "manager",
  "version": "1.0.0",
  "description": "generate questions to bring to your 1-on-1",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-sass --omit-source-map-url main.scss -o public/ && cp -r index.html questions.json main.js woff public"
  },
  "author": "Emil",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.13.1"
  }
}
